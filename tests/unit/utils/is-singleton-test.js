import isSingleton from 'dummy/utils/is-singleton';
import { module, test } from 'qunit';

module('Unit | Utility | is singleton');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isSingleton();
  assert.ok(result);
});
